The RF model runs entirely on the GEE platform, while the GDNN model is trained locally through an integrated geemap interface. This combination allows us to fully leverage GEE’s vast data resources and computational power, while taking advantage of the strong modeling capabilities and nonlinear feature representation of the local deep learning environment, thereby achieving high-accuracy identification and generalized modeling of complex lithology types.
